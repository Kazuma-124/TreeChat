# TreeChat 服务端环境变量
#
# 重要：API 方案（base_url / api_key / model）已不再通过 .env 静态配置，
# 而是由用户在浏览器「⚙ API」界面中保存并启用。本文件只保留运维相关项。

# 服务端口
PORT=3001

# 离线 mock（可选调试开关）：设为 1 时所有对话走本地假回答，不调用任何 LLM。
# 置空或 0 走真实 LLM（需用户在「⚙ API」中已启用一个方案）。
MOCK_LLM=0
