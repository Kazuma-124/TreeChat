# 依赖
node_modules/

# 本地数据库（SQLite，自动重建，勿提交）
server/data/

# 密钥 / 本地配置
.env
server/.env
server/.masterkey

# 构建产物
dist/
dist-ssr/
*.local

# 日志与系统文件
*.log
.DS_Store

# 工具 / 临时导出残留
.claude/
_d_tree.json
_d_*.json
_t_*.json

# 开发自用目录（本地笔记 / 部署指南等，不入库）
develop_use/
