开发一个可以配置自己的ai api的对话应用。

可以实现对某个具体回答进行子提问，让子提问和对应回答，也就是子对话作为当前对话的子节点附加在当前对话之下。

而不是像传统的线性对话应用一样，新对话总是与当前对话平级，会跟在当前对话后面导致对某个回答的提问会把之前的对话顶上去，新提问看完后向返回之前的回答只能往前翻，就很麻烦。

本应用的一问一答形成一个节点。节点之间可以是父子的树形关系，也可以是同属一个父节点的平级关系，也可以没有关系。对某个回答的子提问与对字提问的回复形成子节点，该回答与该回答的提问是父节点。

同时关于上下文，能够实时整理各个节点的上下文。整理机制是将一对用户的提问和对应的回答的节点与上下文的基本元素一一对应，根据每个节点的内容生成对应的描述该节点内容的大致文本，同时给该节点打上标签，加上产生的时间以及与别的基本元素的关系(父节点，子节点，属于那个树)，这些东西形成该节点的上下文元素。所有上下文元素加上标识ID形成一张上下文元素总表，每个问答节点又有自己的上下文表记录了该问答节点提问时使用来哪些节点的上下文元素，是上下文元素总表的子集。

每次对用户的提问选取配套上下文时都先把用户提问所属的对话节点的父节点的上下文元数据表和总的所有上下文元素元数据表以及用户的提问综合发给API让API根据里面的内容从所有上下文元素元数据表中选取与本次提问有关的上下文，然后应用根据API的回复决定本次用户提问应该包含哪些上下文元素，然后把用户的提问与它所需的上下文发给api，进行实际的提问。
你根据上面的设想结合市面上现有类似产品的设计经验给我搞一个方案出来。