开发一个可以配置自己的ai api的对话应用。

可以实现对某个具体回答进行子提问，让子提问和对应回答，也就是子对话作为当前对话的子节点附加在当前对话之下。

而不是像传统的线性对话应用一样，新对话总是与当前对话平级，会跟在当前对话后面，传统的线性对话AI的所有的对话都是平级的，我这个应用的AI的回答之间是有类似树的关系的。平级的AI回答之间是类似数组或者链表的平行关系，对某个回答的子提问所产生的子回答与该父回答是树中的子节点与父节点的关系。
同时关于上下文，能够实时整理各个对话的上下文。整理机制是将一对用户的提问和对应的回答作为组成上下文的基本元素，给每个基本元素打上标签和产生的时间以及与别的基本元素的关系(父节点，子节点，属于那个树)。将所有元素的标识ID与元数据信息(标签，时间，对话树的关系，上下文内容的简介)形成一张表，每次对用户的提问选取配套上下文时都先把用户提问所属的对话节点的父节点的上下文元数据表和总的所有上下文元素元数据表以及用户的提问综合发给API让API根据里面的内容从所有上下文元素元数据表中选取与本次提问有关的上下文，然后应用根据API的回复决定本次用户提问应该包含哪些上下文元素，然后把用户的提问与它所需的上下文发给api，进行实际的提问。
你根据上面的设想结合市面上现有类似产品的设计经验给我搞一个方案出来。