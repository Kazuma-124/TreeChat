# 开发计划及日志 · 资源（图片/大段文本/代码）识别与上下文方案

> 本文件记录 TreeChat 「资源识别 + 两阶段上下文升级」功能的开发计划与进度日志。
> 详细设计见对话讨论结论（v2 方案）。本文件只记录「分阶段做什么、做到哪了」。

---

## 一、目标概述

让对话树支持用户附带**资源**（上传图片/文件、复制的大段文本或代码）发起提问，并由 AI 基于资源作答；同时不破坏现有树形结构、两阶段上下文引擎与透明度机制。

核心设计决策（v2）：
1. **资源 ≠ 提问**：资源从 `user_message` 剥离，存为节点独立 `resources` 结构；`user_message` 只留用户手打提问。
2. **资源元数据**：处理时让模型为每份资源生成「简介 + 标签」，存为资源元数据。
3. **阶段一职权扩大**：既选节点，又把「当前提问」拆成「手写提问 + 资源」，并对每个被选节点、当前资源决定**是否把资源原文纳入上下文**（默认严格：尽量不纳，必要时仅描述，极必要才原文）。
4. **灵活标准**：阶段一先判断「此提问需不需要上下文」，不需要则直接返回空。
5. **视觉/模块模型 = 主模型配置的一部分**：在主配置里声明「搭配哪些模块模型、干什么、是否启用」，可存多套配置或临时开关。
6. **存储 = base64**（DB 内联，支持多图，免文件服务）；**输入区分手写栏与资源栏**。

---

## 二、分段计划

### 阶段 1 · 地基（零行为变化，最安全）✅ 已完成
- `db.js`：新增列 `context_elements.resources`、`context_elements.has_resource`、`api_configs.paired_models` + 迁移。
- `apiConfig.js`：读写 `paired_models`（list/get/create/update 同步）。
- `src/api.ts`：类型同步（`ApiConfig.paired_models`、`ContextElement.resources/has_resource`、`Resource` 接口）。
- 本阶段只加字段、不被任何逻辑使用，现有对话完全不受影响。

### 阶段 2 · 资源接入与存储 ✅ 已完成
- 前端 composer 拆成「手写栏 + 资源栏（右上角 chip，多图/多资源，base64）」；粘贴 >200 字自动转资源。
- `chat.js`：接收 `resources`；`messageProcessor` 落库 `resources`/`has_resource` 并用主模型生成每份资源的「简介+标签」（汇入节点 summary/tags）。
- `chatGenerator`：当前轮支持多模态（图片 image_url / 文本代码作为附加文本块）；新增 `generateResourceMetas`。
- `ConversationCard`：渲染资源 chip / 缩略图 + 描述。
- 此时尚未改上下文引擎，旧逻辑照跑，仅节点多了资源数据。

### 阶段 3 · 两阶段引擎升级（最高风险）
- `contextRetriever`：产出结构化计划 `context_intent` + `node_plan` + `current_resource_plan` + `reasoning`（含灵活标准、资源严格默认不带）。
- `contextBuilder`：按计划组装（节点带原文/仅描述/不带；当前资源 raw/desc/omit）。
- `chatGenerator`：支持当前轮多模态 + 按档带资源。
- 放前面稳定后再改，便于定位问题。

### 阶段 4 · 视觉/模块模型作为配置 + 设置 UI
- `moduleService`（泛化原 visionService）。
- 主配置 `paired_models` 接线 + 启用勾选；`ApiSettings` 管理界面。
- `ContextSourcePanel` 展示阶段一计划与理由。

### 阶段 5 · 收尾
- MOCK 模式假数据、导入/导出含资源、搜索覆盖资源描述、边界与失败回退。

---

## 三、开发日志

| 日期 | 阶段 | 内容 | 状态 |
| --- | --- | --- | --- |
| 2026-08-14 | 清理 | 删除 3 个旧设计方案 MD（树形对话AI应用设计方案1/2、MVP合并设计方案），新建本计划文件 | 完成 |
| 2026-08-14 | 阶段 1 | db.js 加列 + 迁移；apiConfig.js 读写 paired_models；api.ts 类型同步 | 完成（已通过语法检查与迁移冒烟测试） |
| 2026-08-14 | 阶段 2 | 资源接入与存储：composer 拆手写/资源栏、多图 base64、粘贴转资源；chat.js/messageProcessor/chatGenerator 落库并用模型生成资源元数据；卡片渲染资源 | 完成（tsc 通过、服务端启动正常） |
